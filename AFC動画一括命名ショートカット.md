# AFC動画一括命名（iPadショートカット）

初回だけ、iPadの「ショートカット」アプリで名前を **AFC動画一括命名** にして、次の流れを作る。

1. 「クリップボードを取得」
2. 「入力から辞書を取得」
3. 辞書の `items` を取得する
4. 「写真を選択」を追加し、`複数を選択` をオン、メディアタイプを `ビデオ` にする
5. 選択した動画を「作成日・古い順」で並べ替える
6. 動画を繰り返す。繰り返し番号と同じ順番の `items` を取り出し、その中の `fileName` を取得する
7. 動画に「名前を設定」を実行し、取得した `fileName` を指定する
8. 「ファイルを保存」で `このiPad内/AFC Upload` などの専用フォルダへ保存する

## 当日の使い方

1. 撮影中はアプリを操作せず、普通に動画を撮る。
2. 試合後、アプリの該当試合詳細で「この日の動画をまとめて命名」を押す。
3. コピー済みと表示されたら「ショートカットを開く」を押す。
4. 撮影した動画を、撮影した順に選ぶ。
5. `AFC Upload` の名前付き動画をYouTubeへまとめてアップロードする。

選ぶ動画の本数は、アプリに表示された本数と合わせる。通常は各試合の通し動画が自動で対象になる。前半・後半・PKを別動画で撮った場合だけ、試合詳細の「追加動画」からその種類を一括命名リストへ追加する。
